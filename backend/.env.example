# Example environment variables for backend serverless functions
# Copy this file to `.env` (if using locally) or add these vars to your hosting provider.

# MongoDB connection string (MongoDB Atlas). Example:
# mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=

# Optional: database name (defaults to `worksheet` if not set)
MONGODB_DB=worksheet
